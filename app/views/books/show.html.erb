<p id="notice"><%= notice %></p>

<p>
  <strong><%= t('label.title') %>:</strong>
  <%= @book.title %>
</p>

<p>
  <strong><%= t('label.memo') %>:</strong>
  <%= @book.memo %>
</p>

<p>
  <strong><%= t('label.author') %>:</strong>
  <%= @book.author %>
</p>

<p>
  <strong><%= t('label.picture') %>:</strong>
  <%= image_tag(@book.picture_url) if @book.picture.present? %>
</p>

<%= link_to t('button.edit'), edit_book_path(@book) %> |
<%= link_to t('button.back'), books_path %>
